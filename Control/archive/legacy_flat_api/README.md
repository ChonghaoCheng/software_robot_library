# Legacy flat Control API

These forwarding headers preserve the flat include layout used before Control
was split into `Core`, `TrajectoryTracking`, and `Contact`.

They are archived migration references only. CMake does not compile or install
this directory; consumers must include the canonical categorized headers.
